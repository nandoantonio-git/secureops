VITE_API_BASE_URL=http://localhost:8000
VITE_DASHBOARD_REPOSITORY=secureops/example-service
# Mirrors the backend GATE_MODE setting (secureops/.env). The dashboard has
# no endpoint for gate mode today, so this is read from config rather than
# queried live -- see SecurityOverviewPage.tsx.
VITE_DASHBOARD_GATE_MODE=advisory
